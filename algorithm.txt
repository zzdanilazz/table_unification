0) открываем общ.таблицу
1) проходимся по вх.таблицам
2) проходимся по строкам (начиная с 3-й) текущей таблицы
3) сохраняем дату (1 столбец), начало смены (4), конец смены (5), наличные (6), эквайринг (7)
и название вх.таблицы в список "days"
4) ищем дату из списка "days" в общ.таблице и сохраняем номер строки с этой датой в переменную "current_string_output"
5) ищем название вх.таблицы из списка "days" в общ.таблице, начиная c "current_string_output"
и заканчивая "current_string_output + 18", и сохраняем номер строки с этим названием в переменную "current_string_output"
6) записываем в "current_string_output"-ю строку в 3-6 столбцы данные из "days" (1-4 элементы) соответственно